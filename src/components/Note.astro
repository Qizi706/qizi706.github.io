---
interface Props {
	title?: string;
	name?: string;
	class?: string;
}

const { title, name, class: className } = Astro.props;
---

<div class:list={['note', className]} data-name={name}>
	<div>
		{title && <h1>{title}</h1>}
		<slot />
	</div>
</div>
