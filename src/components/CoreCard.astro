---
interface Props {
	title?: string;
	href?: string;
	path?: string;
	class?: string;
}

const { title, href, path, class: className } = Astro.props;
---

<div class:list={['code-card', className]} data-path={path}>
	<div>
		<div>
			{title && <h2 class="card-title">{href ? <a href={href}>{title}</a> : title}</h2>}
			<slot />
		</div>
	</div>
</div>
