---
interface Props {
	title?: string;
	class?: string;
}

const { title, class: className } = Astro.props;
---

<div class:list={['slideshow', className]}>
	<div>
		{title && <h2 class="slide-title">{title}</h2>}
		<slot />
	</div>
</div>
