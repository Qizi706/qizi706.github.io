---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';

interface Props {
	title: string;
	description: string;
	mainClass?: string;
	noindex?: boolean;
}

const { title, description, mainClass, noindex = false } = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={title} description={description} {noindex} />
	</head>
	<body>
		<Header />
		<div class="page-shell">
			<main class:list={['page-frame', mainClass]}>
				<slot />
			</main>
		</div>
		<Footer />
	</body>
</html>
